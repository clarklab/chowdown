#!/usr/bin/env bash

DESTINATION=${1:-/Volumes/share/chowdown}
BASEURL=${1:-http://hatouch.local}

if ! [ -d "$DESTINATION" ]; then
  echo "Destination does not exist: $DESTINATION"
  exit 1
fi

echo "Building site to " . $DESTINATION

jekyll build -d $DESTINATION -b $BASEURL --trace
