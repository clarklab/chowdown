name: Create chowdown image for ARM

on:
  push:
    branches: [ publish ]
  pull_request:
    branches: [ publish ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Set environment variables
      run: |
        IMG=chowdown
        echo "name=BUILD_VER::1.0.$GITHUB_RUN_NUMBER" >> $GITHUB_ENV
        echo "name=IMG::${IMG}" >> $GITHUB_ENV
        echo "name=IMAGE::ghcr.io/${{ github.repository_owner }}/${IMG}" >> $GITHUB_ENV
        echo "name=BUILD_DATE::$(date +'%Y-%m-%d %H:%M:%S')" >> $GITHUB_ENV
        echo "name=GIT_SHA::$(echo ${{ github.sha }} | cut -c1-7)" >> $GITHUB_ENV
        echo "name=GIT_REF::$(git symbolic-ref -q --short HEAD || git describe --tags --exact-match)" >> $GITHUB_ENV
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1
      with: 
        registry: ghcr.io
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    - name: Build and push the Docker image
      uses: docker/build-push-action@v2
      with:
        labels: |
          org.opencontainers.image.authors=${{ github.repository_owner }}
          org.opencontainers.image.created=${{ env.BUILD_DATE }}
          org.opencontainers.image.description=Created from commit ${{ env.GIT_SHA }} and ref ${{ env.GIT_REF }}
          org.opencontainers.image.ref.name=${{ env.GIT_REF }}
          org.opencontainers.image.revision=${{ github.sha }}
          org.opencontainers.image.source=https://github.com/${{ github.repository }}
          org.opencontainers.image.version=${{ env.BUILD_VER }}
        push: true
        secrets: |
            GIT_AUTH_TOKEN=${{ secrets.DOCKER_PASSWORD }}
